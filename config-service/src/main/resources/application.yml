server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/AlexiSkyline/microservices-garage
          search-paths: config-data
  application:
    name: config-service